# Normalize all text files to LF in the repo and on checkout (matches
# .editorconfig: end_of_line=lf). Binary content is auto-detected and skipped.
* text=auto eol=lf

# Windows batch/cmd shims must stay CRLF for cmd.exe.
*.cmd text eol=crlf
*.bat text eol=crlf
