---
description: Show tag, file, and component frequency trends across the RCA corpus.
allowed-tools: Bash
---

Run: `claude-rca trends`

Present the output as:

1. Total RCA count
2. Top 5 tags (with counts)
3. Top 5 files (with counts) — these are the most bug-prone files
4. Recurrent files (appeared in ≥2 RCAs) — highlight these as hotspots
